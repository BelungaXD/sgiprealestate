# Database Configuration
# Option 1: Local PostgreSQL (if you have PostgreSQL installed locally)
# DATABASE_URL="postgresql://postgres:password@localhost:5432/sgip_real_estate?schema=public"

# Option 2: Docker PostgreSQL (use this if you're running docker-compose)
DATABASE_URL="postgresql://postgres:password@localhost:5432/sgip_real_estate?schema=public"

# Next.js Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# SQLite Database (for quick start without PostgreSQL)
# Uncomment the line below and comment out PostgreSQL DATABASE_URL above
# DATABASE_URL="file:./dev.db"
