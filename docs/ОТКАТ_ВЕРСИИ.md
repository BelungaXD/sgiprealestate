# 🔄 Как откатить код до предыдущей версии

> 📖 **См. также**: [INDEX.md](./INDEX.md) - Индекс всей документации

## Вариант 1: Просмотр истории коммитов

Сначала посмотрите историю изменений:

```bash
git log --oneline
```

Или с более подробной информацией:
```bash
git log --oneline --graph --all -20
```

Вы увидите список коммитов с их хешами (например, `abc1234`).

## Вариант 2: Откат к конкретному коммиту

### Безопасный способ (создает новый коммит):

```bash
# 1. Найдите хеш коммита, к которому хотите вернуться
git log --oneline

# 2. Откатитесь к этому коммиту (создаст новый коммит)
git revert <хеш_коммита>
```

### Полный откат (удаляет изменения):

```bash
# 1. Найдите хеш коммита
git log --oneline

# 2. Откатитесь к этому коммиту
git reset --hard <хеш_коммита>
```

⚠️ **Внимание:** `git reset --hard` удалит все незакоммиченные изменения!

## Вариант 3: Откат последнего коммита

```bash
# Отменить последний коммит, но оставить изменения
git reset --soft HEAD~1

# Отменить последний коммит и удалить изменения
git reset --hard HEAD~1
```

## Вариант 4: Откат нескольких коммитов назад

```bash
# Откатиться на 3 коммита назад (оставить изменения)
git reset --soft HEAD~3

# Откатиться на 3 коммита назад (удалить изменения)
git reset --hard HEAD~3
```

## Вариант 5: Откат конкретного файла

Если нужно откатить только один файл:

```bash
# Откатить файл к версии из последнего коммита
git checkout HEAD -- путь/к/файлу

# Откатить файл к версии из конкретного коммита
git checkout <хеш_коммита> -- путь/к/файлу
```

## Вариант 6: Просмотр изменений перед откатом

Перед откатом можно посмотреть, что изменилось:

```bash
# Посмотреть изменения в конкретном коммите
git show <хеш_коммита>

# Посмотреть изменения между коммитами
git diff <старый_хеш> <новый_хеш>
```

## Вариант 7: Создание новой ветки из старой версии

Если хотите сохранить текущую версию и вернуться к старой:

```bash
# Создать новую ветку из старого коммита
git checkout -b старая-версия <хеш_коммита>

# Вернуться к основной ветке
git checkout main
# или
git checkout master
```

## Полезные команды

```bash
# Посмотреть все ветки
git branch -a

# Посмотреть изменения в рабочей директории
git status

# Посмотреть, какие файлы изменены
git diff --name-only

# Сохранить текущие изменения перед откатом
git stash
git reset --hard <хеш_коммита>
# Вернуть изменения обратно (если нужно)
git stash pop
```

## Примеры

### Пример 1: Откат последнего коммита
```bash
git log --oneline
# Вы увидите: abc1234 Последний коммит
#            def5678 Предыдущий коммит

git reset --hard def5678
```

### Пример 2: Откат конкретного файла
```bash
# Откатить только package.json к версии из коммита abc1234
git checkout abc1234 -- package.json
```

### Пример 3: Просмотр и откат
```bash
# 1. Посмотреть историю
git log --oneline -10

# 2. Посмотреть изменения в коммите
git show abc1234

# 3. Откатиться к этому коммиту
git reset --hard abc1234
```

## ⚠️ Важные предупреждения

1. **`git reset --hard`** удаляет все незакоммиченные изменения безвозвратно!
2. Если вы уже отправили коммиты на удаленный репозиторий (GitHub/GitLab), используйте `git revert` вместо `git reset`
3. Перед откатом лучше создать резервную копию или новый коммит

## Безопасный способ (рекомендуется)

```bash
# 1. Сохранить текущие изменения
git stash

# 2. Посмотреть историю
git log --oneline

# 3. Откатиться к нужному коммиту
git reset --hard <хеш_коммита>

# 4. Если нужно вернуть сохраненные изменения
git stash pop
```

